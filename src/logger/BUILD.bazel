load("@rules_cc//cc:defs.bzl", "cc_library")

cc_library(
    name = "logger_init",
    hdrs = [
        "logger_init.h",
    ],
    deps = [
        "@spdlog//:spdlog"
    ],
    visibility = ["//visibility:public"],
)